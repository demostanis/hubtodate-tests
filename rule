[repository]
owner = demostanis
name = hubtodate-tests

[release]
match = main
install = mv -f main /usr/local/bin/hubtodate-tests && \
  chmod +x /usr/local/bin/hubtodate-tests && \
  hubtodate-tests

[verification]
sha256sums = SHA256sums.txt
signature = main.gpg 

